// components/hydrate/EnsureServersClient.tsx
"use client";

import { useEnsureServers } from "@/components/hooks/use-ensure-servers";

export const EnsureServersClient = () => {
  useEnsureServers();

  return null;
};
